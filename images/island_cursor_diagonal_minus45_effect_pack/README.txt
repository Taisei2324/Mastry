Island cursor loading effect — reference style

What changed:
- 45-degree custom cursor still uses your extracted Island logo.
- Click/hover creates a big loading-screen style word burst.
- The burst is split into vertical slices.
- Each slice sequentially zooms in from left to right.
- Added neon pink/yellow/blue chromatic shadow like the reference image.
- Added italic/skewed motion and scanline jitter.

Use:
Upload these files to your website and include the HTML blocks, CSS, and JS.
Edit --cursor-size and --cursor-angle in style.css.
